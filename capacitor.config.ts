import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'Registraap2.0',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
